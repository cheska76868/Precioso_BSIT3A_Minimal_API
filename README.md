# Minimal API

A **Product Management System** REST API built with ASP.NET Core 8, Entity Framework Core, and SQL Server. Provides full CRUD operations for Products, Categories, Suppliers, and Customers with Swagger UI for testing.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Framework | ASP.NET Core 8 Web API |
| ORM | Entity Framework Core 8 |
| Database | SQL Server |
| API Docs | Swagger / OpenAPI |
| Language | C# (.NET 8) |

---

## Project Structure

```
ProductAPI/
├── Controllers/
│   ├── ProductController.cs
│   ├── CategoryController.cs
│   ├── SupplierController.cs
│   └── CustomerController.cs
├── Data/
│   └── ApplicationDBContext.cs
├── Models/
│   ├── Product.cs
│   ├── Category.cs
│   ├── Supplier.cs
│   └── Customer.cs
├── Migrations/               ← auto-generated by EF Core
├── appsettings.json
└── Program.cs
```

---

## Models & Relationships

```
Category ──< Product >── Supplier

Customer (independent)
```

### Product
| Property | Type | Notes |
|---|---|---|
| `ProductId` | int | Primary Key |
| `Name` | string | Required |
| `Description` | string | |
| `Price` | decimal | Range: 1 – 1,000,000 |
| `Stock` | int | Range: 0 – 10,000 |
| `CategoryId` | int | Foreign Key → Category |
| `SupplierId` | int | Foreign Key → Supplier |

### Category
| Property | Type | Notes |
|---|---|---|
| `CategoryId` | int | Primary Key |
| `Name` | string | Required, max 100 chars |
| `Description` | string? | Max 255 chars |
| `Products` | ICollection\<Product\> | Navigation property |

### Supplier
| Property | Type | Notes |
|---|---|---|
| `SupplierId` | int | Primary Key |
| `Name` | string | Required, max 150 chars |
| `ContactEmail` | string? | Max 200 chars |
| `Phone` | string? | Max 20 chars |
| `Address` | string? | Max 300 chars |
| `Products` | ICollection\<Product\> | Navigation property |

### Customer
| Property | Type | Notes |
|---|---|---|
| `Id` | int | Primary Key |
| `FirstName` | string | Required, max 100 chars |
| `LastName` | string | Required, max 100 chars |
| `Email` | string? | Max 200 chars |
| `Phone` | string? | Max 20 chars |
| `Address` | string? | Max 300 chars |

---

## Getting Started

### Prerequisites
- [.NET 8 SDK](https://dotnet.microsoft.com/download)
- SQL Server (LocalDB, Express, or full)
- Visual Studio 2022

Install EF Core CLI tools if not already installed:
```bash
dotnet tool install --global dotnet-ef
```

---

### 1. Configure the Database Connection

Edit `appsettings.json` and set your SQL Server instance:

```json
"ConnectionStrings": {
  "DefaultConnection": "Server=YOUR_SERVER_NAME;Database=ProductDB;Trusted_Connection=True;TrustServerCertificate=True;"
}
```

| Setup | Server Name |
|---|---|
| LocalDB | `(localdb)\\MSSQLLocalDB` |
| SQL Server Express | `.\\SQLEXPRESS` |
| Full SQL Server | `localhost` |

### 2. Apply Migrations & Create the Database

```bash
dotnet ef migrations add InitialCreate
dotnet ef database update
```

> ⚠️ **If you get a foreign key conflict error** on first run, delete the `Migrations` folder, drop the database in SQL Server Management Studio, then re-run the two commands above.

### 3. Run the Project

```bash
dotnet run
```

Swagger UI will be available at `https://localhost:{PORT}/swagger`.

---

## API Endpoints

### Products — `/api/Product`

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/Product` | Get all products (with Category & Supplier) |
| GET | `/api/Product/{id}` | Get product by ID |
| POST | `/api/Product` | Create a new product |
| PUT | `/api/Product/{id}` | Update a product |
| DELETE | `/api/Product/{id}` | Delete a product |

### Categories — `/api/Category`

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/Category` | Get all categories (with Products) |
| GET | `/api/Category/{id}` | Get category by ID |
| POST | `/api/Category` | Create a new category |
| PUT | `/api/Category/{id}` | Update a category |
| DELETE | `/api/Category/{id}` | Delete a category |

### Suppliers — `/api/Supplier`

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/Supplier` | Get all suppliers (with Products) |
| GET | `/api/Supplier/{id}` | Get supplier by ID |
| POST | `/api/Supplier` | Create a new supplier |
| PUT | `/api/Supplier/{id}` | Update a supplier |
| DELETE | `/api/Supplier/{id}` | Delete a supplier |

### Customers — `/api/Customer`

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/Customer` | Get all customers |
| GET | `/api/Customer/{id}` | Get customer by ID |
| POST | `/api/Customer` | Create a new customer |
| PUT | `/api/Customer/{id}` | Update a customer |
| DELETE | `/api/Customer/{id}` | Delete a customer |

---

## Sample Request Payloads

### POST `/api/Category`
```json
{
  "name": "Electronics",
  "description": "Electronic gadgets and devices"
}
```

### POST `/api/Supplier`
```json
{
  "name": "Tech Supplies Co.",
  "contactEmail": "info@techsupplies.com",
  "phone": "09171234567",
  "address": "123 Main St, Manila"
}
```

### POST `/api/Product`
> ⚠️ Create a Category and Supplier first — `categoryId` and `supplierId` must already exist.
```json
{
  "name": "Wireless Mouse",
  "description": "Ergonomic wireless mouse",
  "price": 599.99,
  "stock": 50,
  "categoryId": 1,
  "supplierId": 1
}
```

### POST `/api/Customer`
```json
{
  "firstName": "Juan",
  "lastName": "Dela Cruz",
  "email": "juan@example.com",
  "phone": "09181234567",
  "address": "456 Rizal Ave, Cebu"
}
```

---

## CORS

The API is configured to allow all origins, methods, and headers via the `AllowAll` CORS policy defined in `Program.cs`, making it easy to connect from any frontend during development.
